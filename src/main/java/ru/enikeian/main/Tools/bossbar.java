package ru.enikeian.main.Tools;

import org.bukkit.boss.BossBar;
import org.bukkit.boss.KeyedBossBar;

public class bossbar {
    public static void warmup(Runnable to, int seconds) {
        // После промежутка времени запускает функцию
        // Но действует как разминка на карте

        BossBar bar; // TODO make a warmup function
    }
}
